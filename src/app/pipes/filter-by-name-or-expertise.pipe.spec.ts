import { FilterByNameOrExpertisePipe } from './filter-by-name-or-expertise.pipe';

describe('FilterByNameOrExpertisePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNameOrExpertisePipe();
    expect(pipe).toBeTruthy();
  });
});
